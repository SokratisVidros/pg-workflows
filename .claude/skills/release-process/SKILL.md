---
name: release-process
description: Prepare deterministic repository releases with a fixed format for version bumps, changelog entries, release commits, tags, and GitHub releases. Use when the user asks to cut a release, bump version, tag a release, or publish release notes.
---

# Release Process

Load and follow the shared release specification at:

`/.shared/skills/release-process.md`

Rules:

- Treat the shared file as the authoritative release process.
- Do not diverge from its commit, tag, and changelog templates.
- Keep this wrapper minimal so the release logic is defined once.
