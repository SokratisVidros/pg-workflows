Before finalizing the first plugin for emails:

1. Convert this repo to a monorepo powered by Bun and Turbo
2. Structure each plug-in to work as a separate package so as not to bloat the core package with plug-in dependencies