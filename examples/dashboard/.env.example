# PostgreSQL connection string. The engine creates its own tables and an
# isolated pg-boss schema, so point this at a scratch database.
DATABASE_URL=postgres://postgres:42424242@localhost:5432/pg_workflows_example
